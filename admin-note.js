// ─── ПАТЧ: admin bypass для checkDemoAccess ───────────────────────────────────
// Этот файл патчит index.js — добавляет обход лимитов для администратора.
// Не используется напрямую — логика встроена в index.js через ADMIN_TG_ID.
export const ADMIN_BYPASS_NOTE = "admin (109664871) bypasses all demo limits";
